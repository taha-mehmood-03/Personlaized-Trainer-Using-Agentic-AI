// Dynamic routing for specific sessions
export default function ChatSessionPage({ params }: { params: { sessionId: string } }) {
  // Since ChatLayout handles client-side state transitions when clicking
  // sidebar items, this acts as a declarative placeholder for deep links.
  return null
}
